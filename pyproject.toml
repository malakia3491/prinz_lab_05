[project]
name = "lab-03"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.3.3",
    "pytest>=8.4.2",
    "streamlit>=1.50.0",
]

[tool.pytest.ini.options]
python_paths = ["."]
testpaths = ["tests"]